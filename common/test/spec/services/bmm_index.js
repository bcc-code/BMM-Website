'use strict';

describe('Service: bmmIndex', function () {

  // load the service's module
  beforeEach(module('bmmLibApp'));

  // instantiate service
  var bmmIndex;
  beforeEach(inject(function (_bmmIndex_) {
    bmmIndex = _bmmIndex_;
  }));

  it('should do something', function () {
    //expect(!!bmmIndex).toBe(true);
  });

});
