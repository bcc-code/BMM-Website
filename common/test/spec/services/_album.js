'use strict';

describe('Service: bmmFormatterAlbum', function () {

  // load the service's module
  beforeEach(module('bmmLibApp'));

  // instantiate service
  var bmmFormatterAlbum;
  beforeEach(inject(function (_bmmFormatterAlbum_) {
    bmmFormatterAlbum = _bmmFormatterAlbum_;
  }));

  it('should do something', function () {
    //expect(!!bmmFormatterAlbum).toBe(true);
  });

});
