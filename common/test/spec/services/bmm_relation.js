'use strict';

describe('Service: bmmRelation', function () {

  // load the service's module
  beforeEach(module('bmmLibApp'));

  // instantiate service
  var bmmRelation;
  beforeEach(inject(function (_bmmRelation_) {
    bmmRelation = _bmmRelation_;
  }));

  it('should do something', function () {
    //expect(!!bmmRelation).toBe(true);
  });

});
