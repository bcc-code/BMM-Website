'use strict';

describe('Service: bmmTranslator', function () {

  // load the service's module
  beforeEach(module('bmmLibApp'));

  // instantiate service
  var bmmTranslator;
  beforeEach(inject(function (_bmmTranslator_) {
    bmmTranslator = _bmmTranslator_;
  }));

  it('should do something', function () {
    //expect(!!bmmTranslator).toBe(true);
  });

});
