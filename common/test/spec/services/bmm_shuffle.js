'use strict';

describe('Service: bmmShuffle', function () {

  // load the service's module
  beforeEach(module('bmmLibApp'));

  // instantiate service
  var bmmShuffle;
  beforeEach(inject(function (_bmmShuffle_) {
    bmmShuffle = _bmmShuffle_;
  }));

  it('should do something', function () {
    //expect(!!bmmShuffle).toBe(true);
  });

});
