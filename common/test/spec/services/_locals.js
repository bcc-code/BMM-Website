'use strict';

describe('Service: _locals', function () {

  // load the service's module
  beforeEach(module('bmmLibApp'));

  // instantiate service
  var _locals;
  beforeEach(inject(function (__locals_) {
    _locals = __locals_;
  }));

  it('should do something', function () {
    //expect(!!_locals).toBe(true);
  });

});
